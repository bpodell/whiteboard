'use strict';

const fibo = require('./lib/solution');

// fibo.iterative(0);
// fibo.iterative(1);
// fibo.iterative(2);
// fibo.iterative(3);
// fibo.iterative(4);
// fibo.iterative(5);
// fibo.iterative(6);
// fibo.iterative(7);
// fibo.iterative(8);

fibo.recursive(0);
fibo.recursive(1);
fibo.recursive(2);
fibo.recursive(3);
fibo.recursive(4);
fibo.recursive(5);
fibo.recursive(6);
fibo.recursive(7);
fibo.recursive(8);